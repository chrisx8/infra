---
- name: Set up container hosts
  hosts: apps01
  become: yes
  vars_prompt:
    - name: miniflux_client_secret
      prompt: "Miniflux - OIDC Client Secret"
  tasks:
  - import_role:
      name: base
  - import_role:
      name: base_vm
  - import_role:
      name: containers
