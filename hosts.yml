all:
  vars:
    ansible_user: root
    ansible_python_interpreter: /usr/bin/python3
    ssl_domain: chrisx.xyz
    timezone: America/Detroit
  children:
    proxmox:
      hosts:
        # Proxmox VE host
        proxmox1:
          ansible_host: proxmox1.arb.chrisx.xyz
    guests:
      hosts:
        # LXC - AlmaLinux 9: AdGuard Home
        adguard:
          ansible_host: adguard.arb.chrisx.xyz
        # VM - AlmaLinux 9: Podman containers
        v-containers:
          ansible_host: v-containers.arb.chrisx.xyz
        # LXC - AlmaLinux 9: Apache + PHP + PostgreSQL server
        postgres:
          ansible_host: postgres.arb.chrisx.xyz
        # VM - AlmaLinux 9: restricted SFTP and SAMBA
        storage:
          ansible_host: storage.arb.chrisx.xyz
