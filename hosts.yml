---
all:
  children:
    cx_container_app:
      hosts:
        ct-adguardhome:
        ct-apache:
        ct-cloudflared:
        ct-gitea:
        ct-jellyfin:
        ct-keycloak:
        ct-miniflux:
        ct-vaultwarden:
    cx_cockpit_kvm:
      hosts:
        kvm-primary:
    cx_postgresql:
      hosts:
        postgres-primary:
    cx_samba:
      hosts:
    hosts_el:
      hosts:
        s-elt800g2:
