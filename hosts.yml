---
all:
  vars:
    ansible_python_interpreter: /usr/bin/python3
    ansible_user: root
    cx_base_el_timezone: America/Detroit
    ssl_domain: chrisx.xyz
  children:
    hosts_el:
      hosts:
        adguard: # LXC - AlmaLinux 9
          ansible_host: adguard.arb.chrisx.xyz
        v-containers: # VM - AlmaLinux 9
          ansible_host: v-containers.arb.chrisx.xyz
        storage: # VM - AlmaLinux 9
          ansible_host: storage.arb.chrisx.xyz
    hosts_pve:
      hosts:
        proxmox1: # Hypervisor - Proxmox VE
          ansible_host: proxmox1.arb.chrisx.xyz
    cx_container_apps:
      hosts:
        container-apache:
        container-cloudflared:
        container-gitea:
        container-jellyfin:
        container-keycloak:
        container-miniflux:
        container-vaultwarden:
