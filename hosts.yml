all:
  vars:
    ansible_user: root
    ansible_python_interpreter: /usr/bin/python3
    timezone: America/Detroit
  hosts:
    # Proxmox VE host
    hyper1:
    apps01:
      ansible_ssh_port: 2201
    # LXC - Ubuntu 21.10: LAMP stack, php apps
    lamp:
    # VM - Ubuntu 20.04: restricted SFTP and SAMBA
    storage:
