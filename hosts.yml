---
all:
  children:
    cx_container_app:
      hosts:
        container-adguardhome:
        container-apache:
        container-cloudflared:
        container-gitea:
        container-jellyfin:
        container-keycloak:
        container-miniflux:
        container-vaultwarden:
    cx_cockpit_kvm:
      hosts:
        s-elt800g2:
    cx_postgresql:
      hosts:
        s-elt800g2:
    cx_samba:
      hosts:
    hosts_el:
      hosts:
        s-elt800g2:
