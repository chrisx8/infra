- name: Install rclone and tree
  ansible.builtin.apt:
    name: [rclone, tree]
    state: present
- ansible.builtin.import_tasks: samba.yml
- ansible.builtin.import_tasks: sftp.yml
- ansible.builtin.import_tasks: cockpit.yml
- ansible.builtin.import_tasks: cockpit_ssl.yml
- ansible.builtin.import_tasks: cockpit_totp.yml
