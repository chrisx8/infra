- name: Create /etc/ssl/chrisx.xyz
  ansible.builtin.file:
    path: /etc/ssl/chrisx.xyz
    state: directory
    mode: 0700
    owner: root
    group: root
- name: Install cert for chrisx.xyz
  ansible.builtin.copy:
    src: "certbot.d/config/live/chrisx.xyz/{{ item }}"
    dest: /etc/ssl/chrisx.xyz
    mode: 0400
    owner: root
    group: root
  loop:
    - fullchain.pem
    - privkey.pem
