{{ _container_user_info.home }}/apache/log/*.log {
	daily
	missingok
	rotate 30
	compress
	delaycompress
	notifempty
	create 644 root root
	sharedscripts
	postrotate
		podman exec -it apache apache2ctl graceful
	endscript
}
