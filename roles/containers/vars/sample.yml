---
container_user: someone

container_specs:
  - name: local
    image: localhost/local_image:latest
  - name: nginx
    image: docker.io/library/nginx:alpine
    ports:
      - 80:80
      - 443:443
    volumes:
      - /etc/ssl:/etc/ssl:ro

container_volumes:
  - vol1
  - vol2
