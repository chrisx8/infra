- name: apt dist-upgrade
  apt:
    update_cache: yes
    upgrade: dist

- name: Install additional packages
  apt:
    name: [apt-transport-https, ca-certificates, curl, dnsutils, fail2ban, git, gnupg, psmisc, vim, unzip]
    state: present

- name: apt cleanup
  import_tasks: apt_post.yml
