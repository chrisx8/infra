- name: Configure Apache site
  template:
    src: bookstack.conf
    dest: /etc/apache2/sites-available/bookstack.conf
- name: Enable bookstack site
  shell: "a2ensite bookstack.conf"

- name: Restart Apache
  systemd:
    name: apache2
    state: restarted
