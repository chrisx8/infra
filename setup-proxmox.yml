---
- name: Customize Proxmox VE
  hosts: proxmox_hosts
  become: true
  vars_prompt:
    - name: ses_user
      prompt: "SES SMTP Username"
    - name: ses_pass
      prompt: "SES SMTP Password"
  tasks:
  - name: Customize Proxmox VE
    include_role: proxmox
  - name: Reboot
    reboot:
