---
- name: Set up systems (BASIC)
  hosts: basic
  become: true
  vars:
    swap_size: 1G
  tasks:
  - name: Run cloud system setup (BASIC)
    include_role:
      name: basic
  - name: Reboot
    reboot:
