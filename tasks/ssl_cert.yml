- name: Install SSL cert fullchain.pem to /etc/ssl/private
  copy:
    src: private/ssl/fullchain.pem
    dest: /etc/ssl/private
    mode: 0400
- name: Install SSL cert privkey.pem to /etc/ssl/private
  copy:
    src: private/ssl/privkey.pem
    dest: /etc/ssl/private
    mode: 0400
